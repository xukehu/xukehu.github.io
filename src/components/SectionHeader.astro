---
const { title, subtitle } = Astro.props;
---
<div class="mb-8">
  <h2 class="text-3xl font-semibold text-ink-900 dark:text-white">{title}</h2>
  {subtitle && <p class="mt-2 text-muted">{subtitle}</p>}
</div>
