---
const { title, subtitle } = Astro.props;
---
<div class="mb-8">
  <h2 class="text-5xl font-semibold text-ink-900 dark:text-white">{title}</h2>
  {subtitle && <p class="mt-4 text-2xl text-muted">{subtitle}</p>}
</div>
